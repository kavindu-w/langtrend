venv/
**/.DS_Store
**/__pycache__/
.pytest_cache/
**/.vscode/
**/node_modules/
**/.astro/
**/dist/
**/coverage/
.coverage
coverage.xml
**/*.log

# Per-week caches contain full paper body text — never publish
# (judge_cache/ is NOT ignored: it holds only verdicts + short quotes and is
# needed for manifest rebuilds in CI)
data/processed/weeks/*/html_cache/
data/processed/weeks/*/pdf_cache/

# Raw PDF downloads
data/raw/pdfs/

# Scratch output from scripts/test_single_paper.py (make test-paper) — full
# paper body text and per-paper experiments, never meant to be committed
data/sandbox/

.claude/

.vercel
.env*
!.env.example
